# 프로젝트 질문 답변

아래는 요청하신 항목들에 대한 요약된 답변입니다.

## 프로젝트 전반
- 프로젝트 목적: 영화 기반 소셜/리뷰 플랫폼을 만들기 위해 시작했습니다. 사용자가 영화 정보를 탐색하고, 리뷰를 남기고, 친구와 소통하도록 지원합니다.
- 맡은 역할: 백엔드와 일부 프론트엔드 연동, 인증/인가, API 설계 및 데이터 모델링을 주로 담당했습니다.
- 팀 기여: 사용자 인증(토큰 기반), 영화/리뷰 모델링, REST API 엔드포인트(시리얼라이저·뷰), CORS/배포 설정, 일부 Vue 컴포넌트와 상태 관리 연동을 구현했습니다.
- 다시 만든다면: 인증 흐름(토큰 → JWT로 전환 고려), 테스트 커버리지 강화, 컴포넌트 분리·문서화, CI 파이프라인과 lint/prettier 자동화 추가.
- 한계점/아쉬운 점: 테스트 부족, 에러 응답 표준화 미흡, 일부 컴포넌트 책임이 과도하게 커짐, 대규모 트래픽 고려한 캐싱/큐 미구현.

## 아키텍처 & 설계
- 전체 아키텍처: 프론트엔드(Vite + Vue 3 + Pinia) —> 백엔드(Django REST Framework, TokenAuth) —> SQLite(개발용). 프론트는 `src`에서 라우팅 및 상태관리, 백엔드는 앱별 Django 구조로 분리되어 있습니다.
- 기술 스택 선정 이유: Django/DRF는 빠른 API 개발과 인증 제공에 유리하며, Vue + Vite는 빠른 개발·핫리로드와 컴포넌트 기반 UI 구성에 적합합니다.
- 프론트–백엔드 데이터 흐름: 사용자가 프론트에서 요청 → `http.js`(axios)로 API 호출 → DRF 뷰가 시리얼라이저 통해 DB와 상호작용 → 응답을 프론트에 반환 → Pinia/컴포넌트에서 상태 갱신.
- 확장성 고려: 앱 분리(`accounts`, `movies`, `reviews`, `boards`)로 기능별 독립성 유지, DRF 시리얼라이저·뷰셋 사용으로 확장 쉬움. 배포시 DB를 PostgreSQL로 전환하고 Redis 캐시 추가하면 확장성 확보 가능.
- 유지보수 구조: 명확한 앱 경계, 시리얼라이저와 뷰 분리, 환경변수 기반 설정(`.env` 사용)으로 환경별 설정 분리.

## 협업 & 코드 품질
- Git 브랜치 전략: `main`(배포용), `develop`(통합 개발), 기능별 `feature/<name>` 브랜치 사용. PR 기반 머지 후 `develop`→`main` 릴리즈.
- 코드 리뷰: PR 템플릿에 체크리스트(테스트, 문서, 영향 범위)를 두고 최소 1명 이상의 리뷰어 승인 후 병합했습니다.
- 의견 충돌 해결: 기술적 근거(성능, 보안, 유지보수성)를 기준으로 토론하고 합의가 안될 경우 프로토타입을 만들어 성능/사용성 비교 후 결정했습니다.

## 프론트엔드 관련 상태 & 구조
- 상태관리: `Pinia` 사용. 인증 토큰·유저 정보·추천 상태 등을 전역으로 관리합니다 (`stores/auth.js`, `stores/recommendation.js`).
- 전역 상태 필요 이유: 로그인 상태, 토큰, 사용자 프로필 등 여러 컴포넌트에서 공유되어야 하는 데이터가 존재하기 때문입니다.
- 컴포넌트 구조: 페이지 뷰 컴포넌트(`views/`)가 있고, 재사용 가능한 UI는 `components/` (현재 워크스페이스에 일부 위치). 뷰는 작은 프레젠테이션 컴포넌트로 분리했습니다.
- 재사용 컴포넌트 기준: 반복되는 UI/로직(예: 카드 리스트, 모달, 로더 등)은 분리해 재사용성과 테스트 용이성 확보.

## 성능 최적화
- 성능 이슈: 초기 개발 단계라 큰 병목 없음. 다만 이미지 로딩과 불필요한 리렌더링은 관찰됨.
- 렌더링 최적화: Vue의 컴포넌트 분리, `key` 사용, 필요 시 `computed`/`watch`로 의존성 최소화.
- 이미지·번들 최적화: 프로덕션 빌드에서 Vite의 코드 분할과 트리 쉐이킹 사용. 큰 이미지는 CDN 또는 외부 스토리지로 분리 권장.

## API & 비동기 처리
- API 통신: Axios 추상화 (`frontend/src/api/http.js`)로 공통 `baseURL`·인터셉터(토큰 자동 포함)를 사용합니다.
- 에러 처리: 각 API 호출에서 try/catch로 잡고 사용자에게 실패 메시지를 표시. 중앙에서 공통 에러 인터셉터를 추가하면 일관성 향상 가능.
- 로딩 상태: 각 컴포넌트에서 `loading` 플래그를 사용하거나 전역 로더를 두어 처리함.
- 서버 느림 대응 UX: 스켈레톤 로더, 즉시 캐시된 데이터(있다면) 보여주기, 그리고 실패 시 재시도 옵션 제공.

## 웹 기본기
- CSR vs SSR: 이 프로젝트는 SPA(클라이언트 사이드 렌더링)로 개발했습니다. 빠른 개발과 클라이언트 라우팅, 리치한 UI 상호작용을 중점으로 선택.
- 브라우저 렌더링 과정(간단요약): HTML 파싱 → DOM 생성 → CSS 파싱 → CSSOM 생성 → 렌더 트리 구성 → 레이아웃 → 페인팅 → 합성.
- CORS 문제: 개발환경에서 프론트(5173)와 백엔드(8000)가 다른 포트로 동작하므로 CORS 설정 필요했습니다. `django-cors-headers`로 `CORS_ALLOWED_ORIGINS`/`CORS_ALLOW_ALL_ORIGINS` 설정으로 해결(개발 전용으로 허용).

## 백엔드 관련 서버 & DB
- REST API 설계 원칙: 리소스 기반 URL, 적절한 HTTP 메서드 사용(GET/POST/PUT/PATCH/DELETE), 상태 코드 준수, 시리얼라이저로 입력/출력 명확화.
- 에러 응답 구조: 현재는 DRF 기본 예외 구조를 사용. 개선 시 `{"error": {"code": "ERR_CODE", "message": "human readable"}}` 형태로 표준화 권장.
- DB 스키마 설계: 앱별 모델 분리. `accounts.User`(`AbstractUser` 확장)에서 `favorite_movies`, `friends` 같은 관계 필드를 추가했습니다. 개발은 SQLite 사용, 운영은 PostgreSQL 권장.

## 인증 & 보안
- 로그인/회원가입 구현: `dj-rest-auth`와 `rest_framework.authtoken`을 사용해 토큰 기반 인증을 구현했습니다. 프론트는 토큰을 `localStorage`에 저장하고 `Authorization: Token <token>` 헤더로 전송합니다.
- 비밀번호 저장(핵심 질문):
  - 이 프로젝트의 `accounts.models.User`는 Django의 `AbstractUser`를 확장한 모델입니다(파일: `backend/accounts/models.py`).
  - Django는 비밀번호를 평문으로 저장하지 않으며, `User.set_password()`를 사용해 해싱합니다. 기본 해싱 알고리즘은 PBKDF2(SHA256)이며, Django 설정에서 `PASSWORD_HASHERS`를 통해 변경·확장할 수 있습니다.
  - 따라서 실제 저장되는 값은 해시된 문자열이며 솔트(salt)와 반복(iterations)이 포함되어 안전하게 저장됩니다.

---

원하시면 이 파일의 내용을 더 상세히 늘리거나, 각 섹션별 예시 코드(예: API 응답 포맷, 시퀀스 다이어그램, Git 브랜치 정책 문서)를 추가로 작성해 드리겠습니다.
